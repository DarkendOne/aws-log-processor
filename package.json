{
  "name": "aws-log-processor",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "aws-log-processor": "./dist/cli.js"
  },
  "scripts": {
    "start": "node dist/cli.js",
    "build": "tsc",
    "dev": "ts-node-dev --respawn src/cli.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^11.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}